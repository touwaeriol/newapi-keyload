# 复制为 .env 并填入真实值（.env 已 gitignore，且不打进镜像）
#
# 注意：naci 登录账号密码不在此配置！它们统一存数据库 config 表，
# 由管理员在系统「配置」页维护（GET/PUT /api/admin/config）。

# 本系统默认管理员登录密钥（仅首次 seed 用；留空则随机生成并打印到容器日志）
ADMIN_ACCESS_KEY=your-admin-login-key

# PostgreSQL 密码（compose 的 db 服务使用）
POSTGRES_PASSWORD=change-me-strong-password

# 可选：naci 平台地址（默认 https://open.naci-tech.com，一般无需改）
# NACI_BASE_URL=https://open.naci-tech.com
